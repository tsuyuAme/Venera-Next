# 更新日志

本项目遵循语义化版本，记录 fork 后的主要变更。

## v1.14.4

### 新增

- WebDAV 漫画库支持通过 `metadata.json` 标记深层目录中的漫画根目录；根目录的直接子目录会作为章节，并使用相对路径作为漫画 ID，避免分类目录下的同名漫画互相覆盖。

- 画师收藏条目支持自动分析：收藏画师时自动搜索所有来源并统计作品标签，生成该画师常用类型/风格（如“常画：校园、恋爱、黑白”）并缓存；条目也可手动重新分析。
- 画师收藏页新增一键全体重新分析（带确认提示，画师间并发执行）；分析结果增至 Top 8，标签统一中文/简体展示并合并简繁变体，修复翻译表 emoji 装饰导致的标签分裂。
- 新增画师收藏页：首页摘要可查看已收藏画师并进入管理页，支持跨源聚合搜索、一键复制画师名和长按删除。画师收藏沿用现有长按漫画详情页画师标签收藏机制，数据随应用设置自动纳入 WebDAV 同步与应用数据导入导出。

### 维护

- 修复 Windows Inno Setup 安装包遗漏 Flutter 原生插件 DLL 的问题；现在会完整收集 Release 目录，避免 `super_native_extensions_plugin.dll` 等新增运行库未随安装包发布。
- 收束漫画展示组件的业务依赖：收藏、历史、本地漫画封面和收藏页显示偏好改由运行时注入，并增加结构检查防止展示层重新依赖具体业务管理器。
- 收束漫画源功能域依赖：阅读历史元数据契约下沉到基础层，数据同步与 WebDAV 附加源改由运行时注入，并增加结构检查防止反向依赖回退。
- 更新 `share_plus` 至 12.0.2，包含 iOS 分享流程的稳定性修复。
- 预处理 Dio 5.11.0 的拦截器兼容性，避免自定义拦截器在异常路径中因缺少 Dio 内部调用链而导致请求挂起。
- 更新 `battery_plus` 至 7.1.1，并通过 Android release 与 macOS DMG 构建验证。

## v1.14.3

### 维护

- 收紧 `path_provider`、`intl` 和 `archive` 的版本约束，并固定 Debian 打包工具依赖及发布工作流使用的 Git commit，降低依赖和构建环境漂移风险。
- 增加 Git 依赖治理说明、可见的 Dart 覆盖率报告、贡献指南、安全政策、行为规范和 Dependabot 配置。
- 合并已通过验证的依赖和 GitHub Actions 更新，并暂缓与当前 `flutter_rust_bridge` 或 Node.js 运行时不兼容的自动升级；`archive`、`image`、`crypto` 和 `uuid` 已更新到兼容版本。
- 将 `sqlite3_flutter_libs` 更新到兼容 `sqlite3 2.x` 的 `0.5.42`，并忽略已停止使用的 `0.6.x` 迁移占位版本，避免破坏桌面端和移动端原生 SQLite 库。
- 发布构建会在编译前重新安装并校验固定的 Rust 工具链及平台目标，避免运行器缓存损坏导致 `cargo` 缺失或并发下载 target 失败；Linux 打包现在也会直接传播 Flutter 构建错误。
- 为 macOS 发布构建增加 Rustup 安装操作的串行保护，避免多个 Rust 插件并行准备 iOS 目标时造成下载文件竞态。

### 修复

- 完善在线漫画源只读加载的瞬时故障恢复：异常 JSON、连接中断和响应截断现在会短暂退避并最多自动重试两次，同时避免重试命中损坏的短期缓存。
- 修复收藏初始化时无条件重建“追更”文件夹并覆盖用户选择的问题；自定义追更文件夹和主动停用状态现在会保留。
- 修复阅读器页码、时间电量和系统状态栏开关绕过设备及漫画专属设置的问题，并在切换状态栏开关后立即应用系统界面模式。
- 完善归档下载选项的异常、空列表和空下载地址处理，避免接口失败时一直加载或弹窗无提示。

## v1.14.2

### 新增

- 收藏页新增独立的封面画廊模式，支持自动或 2～6 列布局，并在本地收藏与网络收藏之间保持一致。

### 修复

- 修复阅读器中手动收藏章节封面后无法取消收藏的问题，同时继续保护收藏其他图片时自动补入的封面。
- 修复 WebDAV 漫画详情只显示前三章的问题；升级后会自动识别并重建受影响版本生成的旧缓存。

## v1.14.1

### 修复

- 修复部分旧版或迁移后的历史数据库缺少单列唯一约束时，新漫画阅读进度和阅读时长无法保存的问题。

## v1.14.0

### 新增

- 新增阅读时长记录与统计：阅读器按前台有效阅读时间累计每部漫画时长，历史页可查看总时长、记录漫画数、阅读最久漫画和时长排行。
- 本地漫画支持导入 PDF 和图片型 EPUB；导入后统一转换为现有本地图片漫画，PDF 逐页渲染并限制输出尺寸，EPUB 按 spine 顺序读取并保留有效元数据和章节导航。
- WebDAV 漫画库增加本地缓存和增量同步：书架优先显示缓存内容，后台对比远端目录后刷新列表，并支持手动立即同步和可配置的自动更新周期。

### 变更

- 在设置首页增加阅读统计入口，并继续保留历史页面中的快捷入口。

### 修复

- 修复 EPUB 导出的图片 manifest 路径缺少扩展名分隔点，以及标题、作者或章节名含 XML 特殊字符时可能生成无效文件的问题。
- 修复在线漫画下载完成后，AVIF 页面会被本地阅读列表过滤而表现为缺页的问题；未知或非图片响应现在会进入重试和错误流程，不再计为下载成功。
- 修复桌面端鼠标悬停标题栏 logo 时，logo 消失并显示 `No Overlay widget found.` 的问题。
- 修复阅读统计摘要在窄屏下标签被强制换行、排行榜时长挤压漫画标题的问题；摘要和排行现在会根据实际内容宽度切换布局。

## v1.13.1

### 新增

- 在“关于”中新增赞助者名单页面，支持置顶赞助、当前赞助者和历史赞助者分区展示。

### 变更

- 赞助者名单改用结构化数据统一生成应用数据、GitHub 名单和 README 置顶宣传区，并增加格式校验、确定性检查及旧版数据兼容。
- 赞助状态改由爱发电 API 定时同步，自动识别当前方案、到期状态、历史档位和符合条件的一次性赞助；同步失败时保留上次成功名单。

### 修复

- 修复桌面端标题栏品牌标识在侧栏折叠时仍显示文字并穿过分割线的问题；折叠状态下仅显示应用 logo。
- 修复开启不可变 GitHub Release 后，预发布构建可能先发布空 Release 导致资产上传失败的问题。
- 修复 Windows 升级后可能无法读取旧应用身份目录中本地数据的问题；迁移过程现在兼容多代目录，并且不会因新目录已生成日志等文件而跳过缺失数据。
- 修复应用配置写入中断或内容异常时直接删除 `appdata.json` 的问题；配置文件现在采用原子替换并保留备份，启动时可从备份或同步数据恢复，同时保留损坏文件用于排查。
- 修复稳定版可能收到预发布版本更新提示的问题；更新检查现在只使用已发布的 GitHub Release，稳定版忽略草稿和预发布，RC 版本才会跟踪后续预发布。

### 文档

- 更新 Windows 安装与分发说明，明确 winget 已正式可用，并补充安装、升级、源刷新、审核延迟和便携版限制。
- 完善赞助档位、永久鸣谢、置顶宣传、排序规则和维护边界说明，并在项目介绍下方恢复漫画源分离声明。
- 新增完整英文 README，并在中英文首页增加语言切换、GitHub 主仓库和 Gitee 国内镜像入口。

## v1.13.0

### 新增

- WebDAV 漫画库支持识别 VeneraNext 单本 CBZ 的解压目录，通过 `metadata.json` 读取标题、作者、标签和章节页码范围，同时保持图片按需加载；无元数据或元数据无效时自动回退普通目录模式。

### 变更

- 统一本地目录、CBZ 和 WebDAV 的漫画图片扩展、封面识别及数字页码排序规则，并将单本归档元数据迁移到共享漫画存储功能域。
- WebDAV 数据同步、漫画归档和在线漫画库统一复用端点认证、客户端创建与远端路径基础设施，同时继续保持三套业务配置和生命周期独立。
- 漫画归档与 WebDAV 在线漫画库设置页复用连接输入字段和 controller 生命周期，保留各自独立的测试、保存和启停逻辑。

### 修复

- 修复 WebDAV 漫画库列表不显示推断封面，以及不可变目录列表异常可能取代漫画文件夹名称的问题；列表与详情现在统一从根目录或首个可读章节推断封面。
- 修复本地漫画仅包含章节子目录且没有根目录封面时无法直接导入，以及非图片文件可能进入本地阅读页列表的问题。
- 修复桌面端折叠侧栏分割线穿过左上角 `VeneraNext` 标识的问题。
- 修复数据同步、本地下载、漫画源、代理设置等界面缺少翻译或同时显示中英文的问题，并增加翻译键完整性检查以防止回退。

### 文档

- 将根 README 调整为面向用户的安装与使用入口，把构建、测试、发布工作流和开发故障排查迁移到独立的中英文开发文档。

## v1.12.0

### 新增

- 为画廊阅读模式增加电子墨水屏刷新，可设置闪烁时长、刷新间隔以及黑色、白色、先白后黑三种刷新样式，以减少电子墨水屏残影。

### 修复

- 修复在线漫画源偶发收到异常或截断的 JSON 响应时直接显示 `SyntaxError: unexpected token` 的问题；只读加载会自动重试一次，并避免将损坏响应写入短期缓存。

## v1.11.0

### 新增

- 为纵向连续和瀑布流阅读模式增加拆分双页功能，可将横向双页图按阅读顺序拆成上下排列，并支持交换拆分顺序。
- 本地漫画 CBZ/ZIP/7Z 导入支持顶层目录下的章节子目录结构，便于导入整部漫画打包后的单文件归档。
- 新增 WebDAV 漫画库在线阅读渠道，仅支持远端目录图片结构按需加载；CBZ/ZIP/7Z 继续作为导入导出、归档和分发格式，不用于在线预览。

### 修复

- 修复瀑布流和纵向连续阅读到章节末尾时，阅读记录可能仍显示剩余 1-2 张图片未读的问题。
- 修复多个 toast 连续出现时位置重叠，导致下方提示被遮挡的问题。
- 修复部分 CBZ/ZIP 使用非 UTF-8 文件名编码时，本地导入可能抛出 `FormatException: Missing extension byte` 的问题。
- 修复部分 Windows 压缩得到的 CBZ/ZIP 在原生解压器和 7z 均失败时，导入提示 `Failed to open archive` 的问题。
- 修复 WebDAV 漫画库读取中文目录或不可变目录列表时，可能显示 `unsupported operation: cannot remove from an unmodifiable list`，以及详情页章节长时间加载的问题。

### 文档

- 按文档类型整理 `doc/` 分类目录，补充中英文文档索引、漫画源开发中文说明和图片增强实验设计记录。
- 扩充 README 使用说明，细化本分支特色、本地漫画导入、CBZ 分发归档、WebDAV 在线漫画库和 WebDAV 数据同步/归档配置区别。
- 明确区分本地漫画压缩包导入导出、WebDAV CBZ 归档和 WebDAV 漫画库在线阅读三类能力。

## v1.10.3

### 新增

- 为 Winget manifest 手动 workflow 增加可选 PR 提交能力，可通过 `WINGET_PKGS_TOKEN` 自动向 `microsoft/winget-pkgs` 创建更新 PR。

### 变更

- 漫画详情页章节排序从易误解的图标按钮改为正序/倒序分段切换，并将选择作为全局偏好持久化。
- 新安装默认不再配置漫画源列表地址，已有用户配置的漫画源列表地址会保留。

### 修复

- 修复生成的 Winget manifest 缺少 schema header，导致 `winget-pkgs` 服务端校验失败的问题。

## v1.10.2

### 新增

- 为 Windows 正式版发布增加 winget manifest 生成脚本、发布工件和手动触发 workflow，为接入 `winget install/upgrade CyrilPeng.VeneraNext` 做准备。

### 修复

- 修复瀑布流模式中，进度条按本章页码定位时可能跳到已预加载的上一章，以及底栏上一章/下一章按钮不切换章节的问题。

## v1.10.1

### 变更

- 将 Dart package 名从 `venera` 迁移为 `venera_next`，使源码包名与 VeneraNext 分支命名保持一致。
- 按功能域重整仓库结构，收拢 `app_shell`、`app_runtime`、`features`、`routing`、`foundation`、`components`、`network` 与测试目录的职责边界。
- 将旧 `pages` 和 `utils` 中的业务页面、平台适配、文件能力、图片处理、初始化协议、并发原语和文本转换能力迁入对应功能域或基础层。
- 为漫画详情、漫画源、阅读器、收藏、图片收藏、设置、同步、本地漫画、搜索、浏览发现和漫画展示组件建立稳定入口，并逐步拆除历史 `part` library。
- 拆除阅读器剩余 `part` library，将阅读器主实现、脚手架、图片视图、手势、加载入口和章节列表改为普通 Dart 实现文件。
- 将搜索自动语言过滤规则从搜索结果页抽出为搜索功能域逻辑，并通过 `features/search/search.dart` 稳定入口暴露。
- 收窄组件层对 `foundation/app.dart` 的依赖，未使用 `App` 单例的组件改为直接引用 UI 扩展入口。
- 收窄阅读器章节评论页对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄聚合搜索页对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄浏览发现分类漫画页和排行榜页对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄首页历史摘要对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄本地漫画下载队列弹窗对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄漫画详情操作按钮、章节列表、评论页、封面查看、评论预览和缩略图对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄设置页入口、本地收藏设置、调试页和日志页对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 收窄漫画源首页摘要和同步漫画归档页对 `foundation/app.dart` 的依赖，改为直接引用 UI 扩展入口。
- 将 `foundation/app.dart` 收窄为 `App` 单例入口，移除 UI 扩展 re-export，并为仍需使用 `App` 的调用点补齐显式 UI 扩展入口。
- 新增项目结构约定文档和结构 import 边界检查，固化入口约束、退场目录和反向依赖规则，防止结构调整回退。

### 修复

- 修复瀑布流模式从章节列表进入靠前页时，预加载上一章可能导致当前章节回跳的问题。
- 修复更新日志页面测试仍断言旧章节标题的问题，并为 CookieJar 测试补充缺少 sqlite3 native library 时的跳过条件。

## v1.10.0

### 新增

- 新增漫画更新提示开关，默认在缩略图角标和漫画详情页提示自上次阅读后的未读更新。
- 新增 `release.json` 作为发布版本号的统一来源，并提供脚本同步校验 `pubspec.yaml`、发布 tag 与更新日志版本章节。

### 变更

- 长按漫画详情页收藏按钮默认快速添加到「追更」收藏夹，同时保留用户设置的有效快捷收藏夹。
- 首页追更栏显示「追更」收藏夹内的漫画封面预览，更新数量仅作为附加提示。
- 漫画详情页阅读记录显示当前章节进度，便于判断已读到总章节中的位置。
- 将各平台发布者、版权、应用 ID 与 AltStore 源身份迁移到 `CyrilPeng/venera-next`；这是破坏性升级，系统会将其视为新的应用身份。
- 将应用展示名、桌面可执行文件、安装目录和发布产物统一命名为 `VeneraNext`；Linux 命令和包名使用 `venera-next`。
- 漫画详情页桌面端与移动端统一为两行按钮布局，阅读统一通过主按钮或章节列表进入。
- 为漫画详情页第二行主按钮补充下载、阅读和继续阅读图标。
- Windows 发布构建会清理旧 runner 输出、声明图标资源依赖并校验图标资源，避免桌面包继续沿用旧图标缓存。
- RC 预发布的 iOS 构建改为使用 Apple 平台兼容的数字短版本，同时保留外部产物文件名中的 RC 版本号。
- Windows 应用元数据中的公司名改为反向域名 `com.github.cyrilpeng`，使个人数据目录使用更规范的 `com.github.cyrilpeng\VeneraNext` 结构，并在新目录为空时自动迁移旧目录数据。
- 缩短 Windows 和 macOS 发行版版权信息，避免属性面板中显示不全。
- 长按漫画详情页收藏按钮后的提示会显示目标收藏夹，避免“已添加”表述不清。

### 修复

- 修复首次启动自动创建「追更」收藏夹后未立即设为默认追更文件夹，导致本轮启动不检查追更的问题。
- 修复删除、移动或重命名追更收藏夹内容后更新角标缓存未及时刷新，导致缩略图更新提示残留的问题。
- 修复正式版用户会收到 RC 预发布版本更新提示的问题，预发布更新仅提示当前使用预发布版本的用户。
- 修复漫画详情页浏览量等只读信息被误作为可点击搜索标签的问题。
- 修复 Windows 便携 ZIP 解压后文件直接散落的问题，改为包含同名顶层目录。
- 修复 Windows ARM64 便携 ZIP 错误使用 x64 构建目录的问题。
- 修复 Arch Linux 预发布包仍按旧可执行文件名 `venera` 打包，导致 `venera-next` 桌面构建失败的问题。
- 修复版本同步脚本匹配空白过宽，避免同步发布元数据时吞掉空行或改写文件换行。
- 修复 Windows 桌面端通过 WebDAV 恢复数据后，旧网络请求实例继续使用已关闭的 Cookie 数据库连接，导致漫画详情页全部加载失败的问题。
- 修复长按追更触发首页历史和本地预览刷新时，封面短暂变成黑色的问题。

## v1.10.0-rc.4

### 修复

- 修复 Arch Linux 预发布包仍按旧可执行文件名 `venera` 打包，导致 `venera-next` 桌面构建失败的问题。

## v1.10.0-rc.3

### 变更

- 将各平台发布者、版权、应用 ID 与 AltStore 源身份迁移到 `CyrilPeng/venera-next`；这是破坏性升级，系统会将其视为新的应用身份。
- 将应用展示名、桌面可执行文件、安装目录和发布产物统一命名为 `VeneraNext`；Linux 命令和包名使用 `venera-next`。
- 漫画详情页桌面端与移动端统一为两行按钮布局，阅读统一通过主按钮或章节列表进入。
- 为漫画详情页第二行主按钮补充下载、阅读和继续阅读图标。
- Windows 发布构建会清理旧 runner 输出、声明图标资源依赖并校验图标资源，避免桌面包继续沿用旧图标缓存。

### 修复

- 修复正式版用户会收到 RC 预发布版本更新提示的问题，预发布更新仅提示当前使用预发布版本的用户。
- 修复漫画详情页浏览量等只读信息被误作为可点击搜索标签的问题。
- 修复 Windows 便携 ZIP 解压后文件直接散落的问题，改为包含同名顶层目录。
- 修复 Windows ARM64 便携 ZIP 错误使用 x64 构建目录的问题。

## v1.10.0-rc.2

### 新增

- 新增 `release.json` 作为发布版本号的统一来源，并提供脚本同步校验 `pubspec.yaml`、发布 tag 与更新日志版本章节。

### 变更

- RC 预发布的 iOS 构建改为使用 Apple 平台兼容的数字短版本，同时保留外部产物文件名中的 RC 版本号。

### 修复

- 修复版本同步脚本匹配空白过宽，避免同步发布元数据时吞掉空行或改写文件换行。

## v1.10.0-rc.1

### 新增

- 新增漫画更新提示开关，默认在缩略图角标和漫画详情页提示自上次阅读后的未读更新。

### 变更

- 长按漫画详情页收藏按钮默认快速添加到「追更」收藏夹，同时保留用户设置的有效快捷收藏夹。
- 首页追更栏显示「追更」收藏夹内的漫画封面预览，更新数量仅作为附加提示。
- 漫画详情页阅读记录显示当前章节进度，便于判断已读到总章节中的位置。

### 修复

- 修复首次启动自动创建「追更」收藏夹后未立即设为默认追更文件夹，导致本轮启动不检查追更的问题。
- 修复删除、移动或重命名追更收藏夹内容后更新角标缓存未及时刷新，导致缩略图更新提示残留的问题。

## v1.9.3

### 变更

- 更换全平台应用图标，基于新设计的 Venera-Next.svg 重新生成 Android、iOS、macOS、Windows、Linux 各尺寸图标资源。
- 追更功能优化：启动时自动创建「追更」收藏夹并设为默认追更文件夹，无需手动配置即可使用追更功能。
- 完善 README，添加 Logo 和详细功能介绍。

### 修复

- 修复检查更新因 jsdelivr CDN 缓存旧版本号导致已发布版本检测不到新版本的问题：改为并行请求 CDN 与 GitHub Release 并取最大版本号，并在发布流程中自动 purge jsdelivr 缓存。

## v1.9.2

### 变更

- 升级 Gradle 版本到 8.14，解决 Flutter 废弃警告。
- 更新 AltStore 源到 v1.9.2 正式版。
- 升级 GitHub Actions（checkout、upload/download-artifact、setup-java、setup-python）至 v5，消除 Node.js 20 弃用警告。
- 调整阅读模式选项顺序，将瀑布流模式置于列表顶部并设为默认阅读模式。
- 调整 Toast 弹窗位置，移动端上移至屏幕下方 1/4 处，避免被设备导航栏遮挡；桌面端保持原位。
- 检查更新弹窗增加新版本号提示，从「有新版本可用」改为「有新版本 x.x.x 可用」。

### 修复

- 修复桌面平台（Linux/Windows）按钮悬停时鼠标光标不显示点击样式的问题。
- 修复 AltStore 源更新工作流在无法自动创建 PR 时标记失败的问题。

## v1.9.1

### 变更

- 正式发布 v1.9.1，整合 RC 阶段验证通过的稳定性修复和发布流程改进。
- 更新日志页面改为按 Markdown 样式渲染，提升标题、列表和行内代码的阅读体验。
- 漫画源多个入口统一归一化 JS 返回值，减少动态 Map/List 类型边界问题。
- 多处后台任务改为排队、显式许可或取消信号驱动，减少固定间隔轮询和退出停顿。

### 修复

- 修复 WebDAV 同步、历史进度、收藏管理、JS 引擎、网络缓存、文件下载和图片下载相关稳定性问题。
- 修复 Windows、Linux/Arch 和 Release 发布链路中的版本号、编译兼容与空产物上传问题。

## v1.9.1-rc.5

### 变更

- 将 Windows MSVC 协程兼容宏提升为全局 CMake 定义，覆盖自定义编译设置的插件目标。

## v1.9.1-rc.4

### 修复

- 修复 Windows 打包脚本严格执行命令后无法解析 `flutter.bat` 的问题。

## v1.9.1-rc.3

### 变更

- Windows 打包脚本改为严格检查构建命令和产物大小，避免失败构建上传空资产。

### 修复

- 修复 Windows 发布构建在 MSVC 14.51 下因协程弃用检查失败的问题。

## v1.9.1-rc.2

### 修复

- 修复 RC 版本号包含连字符时 Arch Linux 包构建失败的问题。

## v1.9.1-rc.1

### 新增

- 发布校验支持 `v1.9.1-rc.1` 形式的 RC 标签，并在 GitHub Release 中自动标记为预发布。

### 变更

- CI 分析流程改为使用锁文件获取依赖，并在分析前运行完整测试。
- SQLite 后台 isolate 操作改为独立打开数据库连接，减少跨 isolate 复用连接带来的稳定性风险。
- 数据导入流程增加替换前备份与失败回滚，并等待相关数据库管理器重新初始化完成。
- 应用数据保存改为排队写入，避免并发保存时的忙等和互相抢占。
- 历史记录异步写入改为 Future 队列，减少阅读进度频繁保存时的轮询等待。
- 历史记录批量刷新改为共享节流并发调度器，简化固定 worker 队列并保留批次节流。
- 关注更新批量检查复用节流并发调度器，减少固定 Channel worker 队列的重复实现。
- 共享节流并发调度器在最后一批任务后不再等待额外节流间隔。
- 收藏批量移动/复制改为事务提交后再通知，并在全文件夹删除提交后刷新计数。
- 收藏空批量移动、复制和删除会提前返回，避免无意义事务和监听通知。
- 本地漫画单项删除收敛为一次本地管理器通知，减少重复刷新。
- WebDAV 数据同步改为显式任务排队，减少上传/下载并发等待时的轮询。
- WebDAV 同步任务等待移除完成后的空延迟，减少排队任务切换时的无意义调度。
- 关闭窗口等待 WebDAV 上传时改为等待当前上传任务，减少退出前的固定间隔轮询。
- 关注更新后台检查改为等待 WebDAV 下载任务完成，避免固定间隔轮询和排队下载竞态。
- 漫画源数据保存改为合并排队写入，减少设置频繁变更时的重复等待。
- 漫画源设置解析与动态读取共用归一化逻辑，并统一包装 JS 回调设置项。
- 漫画源图片加载配置统一标准化，兼容 JS 返回的动态 Map 并拒绝非字符串键。
- 漫画源详情、章节图片和缩略图入口统一归一化 JS 返回值，减少动态 Map/List 类型边界问题。
- 漫画源详情入口补充嵌套字段归一化，兼容 JS 返回的动态推荐、评论和标签数据。
- 漫画源评论和章节评论入口统一归一化 JS 返回值，减少动态评论列表类型边界问题。
- 漫画源归档列表和归档下载地址入口统一校验 JS 返回值，减少归档下载类型边界问题。
- 漫画源搜索、分类和收藏列表入口统一归一化漫画条目，减少动态 Map 类型边界问题。
- 文件下载器改用写入队列串行落盘，减少多块并发下载时的忙等和缓冲累积。
- 缩略图加载并发限制改为显式许可队列，减少高并发加载时的固定间隔轮询。
- 图片响应回调改为统一执行并在异常路径释放，避免 JS 回调引用泄漏。
- 图片加载失败回调改为捕获后统一消费并释放，减少重试配置切换时的引用残留风险。
- 图片加载失败回调兼容 JS Map 返回的动态配置，并拒绝非字符串键配置。
- 图片加载失败重试按剩余预算判断，避免失败回调比预期多执行一次。
- 图片加载重试等待支持取消信号唤醒，减少取消后仍等待重试间隔的停顿。
- 文件下载在任务创建阶段被停止时会关闭输出流和文件句柄，避免调用方等待挂起。
- 文件下载分块失败时会取消已启动分块并保留首个错误，避免错误路径被在途请求阻塞。
- 文件下载断点状态不足时不再继续发送完成状态，避免失败结果被误报。
- 文件下载进度定时器会在失败或提前结束时立即取消，避免关闭流后继续发送进度。
- 图片下载任务暂停或取消时会唤醒重试等待，减少退避延迟造成的暂停卡顿。
- 图片下载任务取消时会停止速度计时器，并在尚未创建下载路径时跳过本地库查询。
- 阅读器自定义图片处理等待改为取消信号驱动，减少固定间隔轮询并传播异步错误。
- 图片脚本处理并发控制改为显式许可队列，减少高频处理时的轮询等待。
- JS 计算池初始化改为共享 Future，并用完成通知替代 isolate 任务关闭轮询。
- 更新日志页面改为按 Markdown 样式渲染，提升标题、列表和行内代码的阅读体验。
- 替换 Flutter 已弃用的 `TickerMode.of` API。

### 修复

- 修复 `Channel` 多个生产者等待同一释放信号时可能突破容量限制的问题。
- 修复文件下载分块请求失败时任务清理和错误传播不稳定的问题，并补充异常路径测试。
- 修复同路径网络请求 `prevent-parallel` 未覆盖完整请求生命周期的问题，并改为 Future 队列减少轮询等待。
- 修复 WebDAV 数据同步失败结果未统一刷新错误状态的问题，并补充异常路径测试。
- 修复共享图片下载取消时未终止底层图片流的问题。
- 修复共享图片流取消后立即重载同图可能复用已关闭流的问题。
- 修复图片下载任务暂停时未取消底层图片流且可能无法唤醒等待者的问题。
- 修复阅读器快速退出时最后阅读进度可能未写入的问题。
- 修复数据导入替换历史库前未等待异步历史写入完成的问题。
- 修复收藏管理器在独立初始化时可能等待未启动的应用数据初始化而挂起的问题。
- 修复收藏哈希刷新后台任务在测试清理临时数据库时可能产生 sqlite 竞态的问题。
- 修复 JS 引擎重置时旧实例未正确释放的问题。
- 修复网络内存缓存可能超过上限的问题，并补充缓存回归测试。
- 修复单条网络缓存超过内存上限时仍会写入缓存的问题。
- 修复缓存清理状态在异常时未复位，导致后续清理任务可能被永久跳过的问题。
- 修复缓存目录初始扫描失败后缓存大小统计长期失效的问题。
- 修复关注更新重试在最后一次失败后仍等待退避间隔的问题。
- 修复历史记录刷新重试在最后一次失败后仍等待退避间隔的问题。

## v1.9.0

### 新增

- WebDAV 数据同步新增状态展示，可显示同步中、上次同步时间和失败信息。
- 新增历史记录自动清理设置，支持按保留天数清理过期历史记录。
- 新增本地漫画导入导出工具层，为后续本地漫画迁移入口提供基础能力。

### 变更

- 统一主要 SQLite 数据库连接配置，设置 journal mode、同步策略和 busy timeout。
- WebDAV 数据同步导入后会刷新历史、收藏和图片收藏状态，减少首页与收藏计数滞后。
- 完善 WebDAV 漫画归档连接测试和错误提示。

## v1.8.1

### 新增

- 关于页面新增更新日志入口，可直接查看 `CHANGELOG.md` 内容。

### 变更

- App 版本号改为从打包内 `pubspec.yaml` 读取，发版时只需维护 `pubspec.yaml`。
- 检查更新改为 CDN 版本信息优先、GitHub 最新发行版兜底，并在失败时提示用户。

## v1.8.0

### 新增

- 新增 WebDAV 本地漫画归档功能，支持备份、恢复、列出和删除本地漫画归档。
- 新增图片收藏图库视图，便于集中浏览和管理单部漫画的图片收藏。
- 新增手动构建工作流，支持按平台选择构建任务。

### 变更

- 增强 tag 自动发布流程，发布前校验版本号、更新日志和关键依赖锁定状态。
- 强化 `flutter_rust_bridge 2.11.1` 锁定检查，降低构建产物无法联网的风险。
- 改进 CBZ 导出兼容性，统一页码文件名、章节范围和 `ComicInfo.xml` 元数据。
- 改进导出文件名处理，增强多语言标题、章节后缀和 UTF-8 长度限制兼容性。
- 简化 README 中的项目说明和构建提示。

### 修复

- 修复发布工作流中 iOS 打包路径硬编码旧仓库名的问题。
- 修复发布工作流中 Linux x64 构建未显式使用锁文件获取依赖的问题。

### 移除

- 移除关于页面中残留的 Telegram 入口。

## v1.7.0

### 新增

- 新增瀑布流阅读模式，作为连续阅读模式的跨章节增强。
- 瀑布流模式复用现有“预加载图片数量”，在当前位置后续图片不足时自动加载下一章。
- 瀑布流阅读时会根据当前可见图片同步章节和阅读进度。

### 变更

- 瀑布流跨章节处增加章节分隔提示，便于识别章节边界。

### 修复

- 修复瀑布流阅读模式名称缺少简繁中文翻译的问题。
- 修复瀑布流跨章节后无法向上滚回上一章的问题。

## v1.6.3

### 新增

- 基于原 Venera 项目创建 fork 初始版本。
- 将原仓库历史压缩为单个初始化提交，作为后续维护基础。
